{% comment %}
  Generic page template for standard content pages.
  Displays page title and flexible block-based content areas.

  Features:
  - Page title with configurable heading size
  - Flexible block system for mixed content
  - Page content block for main copy
  - Support for additional custom blocks
  - Responsive container layout
  - Rich text editor support for page content

  Block types:
  - page_content: Renders the page's main content with RTE formatting
  - Custom blocks: Any additional blocks defined in schema

  Layout:
  - Container with responsive padding
  - Single column grid layout
  - Consistent spacing between elements

  Usage:
  - Standard informational pages (About, Contact, etc.)
  - Landing pages with mixed content
  - Any page requiring flexible content blocks

  Schema settings control:
  - Page heading size options
  - Additional content blocks configuration
{% endcomment %}

<div class='p-pagemargin 2xl:container grid grid-cols-1 gap-y-2xs'>
  <h1 class='{{ section.settings.heading_size }}'>
    {{ page.title | escape }}
  </h1>

  {%- for block in section.blocks -%}
    {% case block.type %}
      {% when 'page_content' %}
        <div class='rte'>
          {{ page.content }}
        </div>
      {% else %}
        {% render block %}
    {% endcase %}
  {%- endfor -%}
</div>

{% schema %}
{
  "name": "t:sections.main-page.name",
  "enabled_on": {
    "templates": ["page"]
  },
  "settings": [
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h2",
          "label": "Heading 2"
        },
        {
          "value": "h1",
          "label": "Heading 1"
        },
        {
          "value": "h3",
          "label": "Heading 3"
        },
        {
          "value": "h4",
          "label": "Heading 4"
        },
        {
          "value": "h5",
          "label": "Heading 5"
        },
        {
          "value": "h6",
          "label": "Heading 6"
        },
        {
          "value": "p",
          "label": "Paragraph"
        },
        {
          "value": "caption",
          "label": "Caption"
        }
      ],
      "default": "h3",
      "label": "t:sections.all.heading_size.label"
    }
  ],
  "blocks": [
    {
      "name": "t:sections.main-page.page_content",
      "type": "page_content",
      "settings": []
    },
    {
      "type": "@app"
    }
  ],
  "presets": [
    {
      "name": "t:sections.main-page.name"
    }
  ]
}
{% endschema %}
