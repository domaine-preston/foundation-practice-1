{% doc %}
  Returns the appropiate color token class for a given color scheme.

  @param {string} color_scheme - Color scheme to apply
{% enddoc %}

color-{{ color_scheme }} bg-background text-foreground
